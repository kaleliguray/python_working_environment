print("Body-Mass Index")

length = float(input("User length: "))
weight = float(input("User weight: "))

body_mass = weight / (length * length)

print("Length: ", length,"\nWeight: ",weight, "\nBody-Mess Index: ",body_mass)
